import { StyleSheet } from 'react-native';

const HeaderStyle = StyleSheet.create({
  container: {
    backgroundColor: 'red',
    marginTop: 36,
    padding: 16,
  },
  text: {
    color: '#fff',
  },
});

export default HeaderStyle;
